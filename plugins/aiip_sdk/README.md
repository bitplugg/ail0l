# AIIA .aiip SDK

The SDK exposes `AiiaPlugin`, `ToolDefinition`, and a deterministic `packageAiip` Gradle task. Run `./gradlew packageAiip`; the resulting archive is written to `build/aiip/example.aiip` with `manifest.json`, `plugin.dex`, and `assets/`.
